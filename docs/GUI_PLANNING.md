# Video Translate Project - CustomTkinter GUI 規劃文檔

## 概述

本文檔規劃使用 [CustomTkinter](https://github.com/TomSchimansky/CustomTkinter) 建立一個整合型主控台 GUI，取代現有的批次檔 + Web UI 操作方式，將所有功能整合到單一桌面應用程式。

---

## 一、視窗架構設計

### 1.1 主視窗規格

```
┌─────────────────────────────────────────────────────────────────┐
│  Video Translate Studio                              [─][□][×]  │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  [翻譯處理]  [字幕編輯]  [位置調整]  [IG文案]  [設定]   │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                                                         │    │
│  │                    Tab 內容區域                          │    │
│  │                    (依據選中的 Tab 顯示)                 │    │
│  │                                                         │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  狀態列：處理進度 / 系統訊息                              │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘

視窗大小: 1200 x 800 (最小 1000 x 700)
主題: Dark Mode (符合現有 Web UI 風格)
```

### 1.2 色彩配置

```python
COLORS = {
    "primary": "#00d4ff",      # 主色調 (青色)
    "secondary": "#7b2cbf",    # 次要色 (紫色)
    "success": "#00ff88",      # 成功 (綠色)
    "warning": "#ffaa00",      # 警告 (橙色)
    "error": "#ff4444",        # 錯誤 (紅色)
    "background": "#1a1a2e",   # 背景深色
    "surface": "#16213e",      # 表面色
    "text": "#ffffff",         # 主文字
    "text_secondary": "#a0a0a0" # 次要文字
}
```

---

## 二、功能模組設計

### 2.1 Tab 1: 翻譯處理 (主功能)

```
┌─────────────────────────────────────────────────────────────────┐
│  📁 輸入影片                                                     │
│  ┌───────────────────────────────────────────────┐ [選擇資料夾] │
│  │ C:\video-translate-project\videos\translate_raw │              │
│  └───────────────────────────────────────────────┘              │
│                                                                 │
│  📋 待處理影片列表                                   [重新掃描]  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ ☑ video_001.mp4                           12.5 MB       │    │
│  │ ☑ video_002.mp4                           8.3 MB        │    │
│  │ ☐ video_003.mp4 (已處理)                  15.2 MB       │    │
│  │ ☑ video_004.mp4                           6.7 MB        │    │
│  └─────────────────────────────────────────────────────────┘    │
│  [全選] [取消全選] [選擇未處理]                                   │
│                                                                 │
│  ⚙️ 處理設定                                                     │
│  ┌─────────────────────┐  ┌─────────────────────┐              │
│  │ Whisper 模型: [base▼]│  │ 執行緒數: [4    ▼] │              │
│  └─────────────────────┘  └─────────────────────┘              │
│  ┌─────────────────────┐  ┌─────────────────────┐              │
│  │ 處理模式: [pipeline▼]│  │ ☑ 強制重新處理      │              │
│  └─────────────────────┘  └─────────────────────┘              │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ [▶ 開始翻譯]                              [⏹ 停止]      │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  📊 處理進度                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 整體進度: ████████████░░░░░░░░ 60% (3/5)                │    │
│  ├─────────────────────────────────────────────────────────┤    │
│  │ 當前: video_002.mp4                                      │    │
│  │ 階段: 翻譯中 ████████░░░░░░░░░░ 45%                      │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  📝 處理日誌                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ [12:30:15] 開始處理 video_001.mp4                        │    │
│  │ [12:30:18] 語音識別完成，共 45 個片段                     │    │
│  │ [12:31:02] 翻譯完成                                      │    │
│  │ [12:31:05] 剪映草稿生成完成                              │    │
│  │ [12:31:06] ✓ video_001.mp4 處理完成                      │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

**功能清單:**
- 選擇輸入資料夾
- 顯示待處理影片列表 (含處理狀態)
- 批量選擇/取消選擇
- Whisper 模型選擇 (tiny/base/small/medium/large-v3)
- 執行緒數設定
- 處理模式選擇 (sequential/parallel/pipeline)
- 強制重新處理選項
- 開始/停止按鈕
- 即時進度顯示
- 處理日誌視窗

---

### 2.2 Tab 2: 字幕編輯

```
┌─────────────────────────────────────────────────────────────────┐
│  📂 選擇字幕檔案                                                 │
│  ┌─────────────────────────────────┐                            │
│  │ video_001_zh.srt              ▼ │  [開啟資料夾]              │
│  └─────────────────────────────────┘                            │
│                                                                 │
│  🔍 搜尋與替換                                                   │
│  ┌───────────────────────────┐  ┌───────────────────────────┐  │
│  │ 搜尋: [                  ]│  │ 替換: [                  ]│  │
│  └───────────────────────────┘  └───────────────────────────┘  │
│  ☑ 使用正則表達式    [尋找下一個] [全部替換] [預覽變更]          │
│                                                                 │
│  📝 字幕內容                                              行數   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 00:00:01,000 --> 00:00:03,500                        1  │    │
│  │ 大家好，歡迎來到我的頻道                                │    │
│  │                                                         │    │
│  │ 00:00:04,000 --> 00:00:07,200                        2  │    │
│  │ 今天我們要討論一個很有趣的話題                          │    │
│  │                                                         │    │
│  │ 00:00:08,500 --> 00:00:12,000                        3  │    │
│  │ 讓我們開始吧                                            │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  [儲存變更] [還原] [匯出 SRT] [匯出 JSON]                        │
└─────────────────────────────────────────────────────────────────┘
```

**功能清單:**
- 字幕檔案選擇下拉選單
- 搜尋/替換功能 (支援正則表達式)
- 即時預覽變更
- 字幕內容編輯區 (可滾動)
- 儲存/還原/匯出功能

---

### 2.3 Tab 3: 位置調整

```
┌─────────────────────────────────────────────────────────────────┐
│  📂 選擇剪映草稿                                                 │
│  ┌─────────────────────────────────┐                            │
│  │ video_001_zh                  ▼ │  [重新整理]                │
│  └─────────────────────────────────┘                            │
│                                                                 │
│  📺 字幕預覽                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                                                         │    │
│  │                                                         │    │
│  │                                                         │    │
│  │             ┌───────────────────────┐                   │    │
│  │             │  這是字幕預覽文字      │                   │    │
│  │             └───────────────────────┘                   │    │
│  │  ▲                                                      │    │
│  │  │ Y軸位置                                               │    │
│  │  ▼                                                      │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  🎚️ 位置控制                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ Y 軸位置:  -400 ═══════════●═══════════ +400            │    │
│  │            當前值: -180                                  │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ⚡ 快速設定                                                     │
│  [📍 下方 (-360)] [📍 中間 (0)] [📍 上方 (+300)]                 │
│                                                                 │
│  🎨 字幕樣式                                                     │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐              │
│  │字體大小: 14 │ │字體顏色: #FFF│ │描邊: 2px   │              │
│  └─────────────┘ └─────────────┘ └─────────────┘              │
│                                                                 │
│  [套用到草稿] [儲存為預設值]                                     │
└─────────────────────────────────────────────────────────────────┘
```

**功能清單:**
- 剪映草稿選擇
- 視覺化字幕位置預覽 (模擬 9:16 畫面)
- Y 軸滑桿調整
- 快速位置按鈕
- 字幕樣式設定 (字體大小、顏色、描邊)
- 套用到草稿功能

---

### 2.4 Tab 4: IG 文案

```
┌─────────────────────────────────────────────────────────────────┐
│  📂 選擇影片                                                     │
│  ┌─────────────────────────────────┐                            │
│  │ video_001                     ▼ │                            │
│  └─────────────────────────────────┘                            │
│                                                                 │
│  📝 字幕摘要                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 影片內容：討論如何提高工作效率的五個技巧...              │    │
│  │ 關鍵字：效率、工作、技巧、時間管理                        │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ✍️ 文案編輯                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 🔥 5個讓你效率翻倍的秘訣！                               │    │
│  │                                                         │    │
│  │ 你是不是常常覺得時間不夠用？                             │    │
│  │ 今天分享我親測有效的5個技巧 👇                           │    │
│  │                                                         │    │
│  │ #效率 #工作技巧 #時間管理 #自我提升                      │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  🏷️ Hashtag 模板                                                │
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐      │
│  │ [生活類]       │ │ [科技類]       │ │ [教學類]       │      │
│  └────────────────┘ └────────────────┘ └────────────────┘      │
│                                                                 │
│  [🤖 AI 生成文案] [📋 複製到剪貼簿] [💾 儲存文案]               │
└─────────────────────────────────────────────────────────────────┘
```

**功能清單:**
- 影片/字幕選擇
- 自動摘要顯示
- 文案編輯區
- Hashtag 模板快速插入
- AI 生成文案 (調用 DeepSeek API)
- 複製到剪貼簿
- 儲存文案

---

### 2.5 Tab 5: 設定

```
┌─────────────────────────────────────────────────────────────────┐
│  ⚙️ Whisper 設定                                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 引擎:        [faster-whisper ▼]                          │    │
│  │ 模型:        [base          ▼]                          │    │
│  │ 設備:        [auto          ▼]  (auto/cuda/cpu)          │    │
│  │ 計算類型:    [int8          ▼]  (float16/int8)           │    │
│  │ ☑ 啟用 VAD 過濾                                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  🌐 翻譯設定                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 翻譯提供者:  [deepseek      ▼]                          │    │
│  │ API Key:     [****************] [顯示] [測試連線]        │    │
│  │ 來源語言:    [English       ▼]                          │    │
│  │ 目標語言:    [繁體中文       ▼]                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  📁 路徑設定                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 輸入資料夾:  [videos/translate_raw    ] [瀏覽]           │    │
│  │ 字幕輸出:    [subtitles               ] [瀏覽]           │    │
│  │ 草稿輸出:    [翻譯專案                 ] [瀏覽]           │    │
│  │ 剪映模板:    [翻譯專案/模板-字幕九宮格 ] [瀏覽]           │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  🎨 字幕樣式預設                                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 字體大小: [14  ]    字體顏色: [#FFFFFF] [選色]           │    │
│  │ 描邊寬度: [2   ]    描邊顏色: [#000000] [選色]           │    │
│  │ 陰影距離: [5.0 ]    Y軸位置:  [-0.45  ]                  │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  🖥️ GPU 資訊                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ CUDA: 可用 ✓    GPU: NVIDIA RTX 3080                     │    │
│  │ 記憶體: 10GB    驅動: 535.104                            │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  [儲存設定] [重置為預設值] [匯出設定] [匯入設定]                 │
└─────────────────────────────────────────────────────────────────┘
```

**功能清單:**
- Whisper 引擎/模型/設備設定
- 翻譯 API 設定
- 路徑設定 (輸入/輸出資料夾)
- 字幕樣式預設值
- GPU 狀態顯示
- 設定儲存/重置/匯出/匯入

---

## 三、檔案結構規劃

```
video-translate-project/
├── gui/
│   ├── __init__.py
│   ├── main.py                    # 主程式入口
│   ├── app.py                     # CTk 主視窗類別
│   │
│   ├── tabs/                      # 各 Tab 模組
│   │   ├── __init__.py
│   │   ├── translate_tab.py       # 翻譯處理 Tab
│   │   ├── subtitle_edit_tab.py   # 字幕編輯 Tab
│   │   ├── position_tab.py        # 位置調整 Tab
│   │   ├── ig_caption_tab.py      # IG 文案 Tab
│   │   └── settings_tab.py        # 設定 Tab
│   │
│   ├── components/                # 可重用元件
│   │   ├── __init__.py
│   │   ├── video_list.py          # 影片列表元件
│   │   ├── progress_panel.py      # 進度面板元件
│   │   ├── log_viewer.py          # 日誌檢視器
│   │   ├── subtitle_preview.py    # 字幕預覽元件
│   │   └── color_picker.py        # 顏色選擇器
│   │
│   ├── utils/                     # GUI 工具函式
│   │   ├── __init__.py
│   │   ├── theme.py               # 主題配置
│   │   ├── threading_utils.py     # 執行緒處理
│   │   └── file_dialogs.py        # 檔案對話框
│   │
│   └── assets/                    # 資源檔案
│       ├── icon.ico               # 應用程式圖示
│       └── icon.png               # PNG 圖示
│
├── translate_video.py             # (現有) 核心翻譯邏輯
├── subtitle_generator.py          # (現有) 字幕生成
├── translation_config.json        # (現有) 配置檔
└── ...
```

---

## 四、類別設計

### 4.1 主應用程式類別

```python
# gui/app.py
import customtkinter as ctk
from tabs import TranslateTab, SubtitleEditTab, PositionTab, IGCaptionTab, SettingsTab

class VideoTranslateApp(ctk.CTk):
    def __init__(self):
        super().__init__()

        # 視窗設定
        self.title("Video Translate Studio")
        self.geometry("1200x800")
        self.minsize(1000, 700)

        # 設定主題
        ctk.set_appearance_mode("dark")
        ctk.set_default_color_theme("blue")

        # 載入配置
        self.config = self.load_config()

        # 建立 UI
        self.setup_ui()

    def setup_ui(self):
        # 建立 TabView
        self.tabview = ctk.CTkTabview(self, width=1160, height=720)
        self.tabview.pack(pady=10, padx=20, fill="both", expand=True)

        # 新增各 Tab
        self.tabview.add("翻譯處理")
        self.tabview.add("字幕編輯")
        self.tabview.add("位置調整")
        self.tabview.add("IG 文案")
        self.tabview.add("設定")

        # 初始化各 Tab 內容
        self.translate_tab = TranslateTab(self.tabview.tab("翻譯處理"), self.config)
        self.subtitle_tab = SubtitleEditTab(self.tabview.tab("字幕編輯"), self.config)
        self.position_tab = PositionTab(self.tabview.tab("位置調整"), self.config)
        self.ig_tab = IGCaptionTab(self.tabview.tab("IG 文案"), self.config)
        self.settings_tab = SettingsTab(self.tabview.tab("設定"), self.config)

        # 狀態列
        self.status_bar = ctk.CTkLabel(self, text="就緒", anchor="w")
        self.status_bar.pack(fill="x", padx=20, pady=(0, 10))

    def load_config(self):
        # 載入 translation_config.json
        pass

    def save_config(self):
        # 儲存配置
        pass
```

### 4.2 翻譯處理 Tab 類別

```python
# gui/tabs/translate_tab.py
import customtkinter as ctk
from threading import Thread
import queue

class TranslateTab:
    def __init__(self, parent, config):
        self.parent = parent
        self.config = config
        self.processing = False
        self.log_queue = queue.Queue()

        self.setup_ui()

    def setup_ui(self):
        # 左側面板 - 影片列表
        self.left_frame = ctk.CTkFrame(self.parent)
        self.left_frame.pack(side="left", fill="both", expand=True, padx=10, pady=10)

        # 資料夾選擇
        self.folder_frame = ctk.CTkFrame(self.left_frame)
        self.folder_frame.pack(fill="x", pady=5)

        self.folder_entry = ctk.CTkEntry(self.folder_frame, width=400)
        self.folder_entry.pack(side="left", padx=5)

        self.browse_btn = ctk.CTkButton(
            self.folder_frame,
            text="選擇資料夾",
            command=self.browse_folder
        )
        self.browse_btn.pack(side="left", padx=5)

        # 影片列表 (ScrollableFrame)
        self.video_list = ctk.CTkScrollableFrame(self.left_frame, height=300)
        self.video_list.pack(fill="both", expand=True, pady=10)

        # 右側面板 - 控制和進度
        self.right_frame = ctk.CTkFrame(self.parent, width=350)
        self.right_frame.pack(side="right", fill="y", padx=10, pady=10)

        # 設定區
        self.setup_settings_panel()

        # 進度區
        self.setup_progress_panel()

        # 日誌區
        self.setup_log_panel()

    def setup_settings_panel(self):
        settings_frame = ctk.CTkFrame(self.right_frame)
        settings_frame.pack(fill="x", pady=10)

        # Whisper 模型選擇
        ctk.CTkLabel(settings_frame, text="Whisper 模型:").pack(anchor="w", padx=10)
        self.model_menu = ctk.CTkOptionMenu(
            settings_frame,
            values=["tiny", "base", "small", "medium", "large-v3"]
        )
        self.model_menu.set(self.config.get("whisper", {}).get("model", "base"))
        self.model_menu.pack(fill="x", padx=10, pady=5)

        # 執行緒數
        ctk.CTkLabel(settings_frame, text="執行緒數:").pack(anchor="w", padx=10)
        self.thread_menu = ctk.CTkOptionMenu(
            settings_frame,
            values=["1", "2", "4", "8"]
        )
        self.thread_menu.set("4")
        self.thread_menu.pack(fill="x", padx=10, pady=5)

        # 開始按鈕
        self.start_btn = ctk.CTkButton(
            settings_frame,
            text="▶ 開始翻譯",
            command=self.start_processing,
            fg_color="#00d4ff"
        )
        self.start_btn.pack(fill="x", padx=10, pady=10)

        self.stop_btn = ctk.CTkButton(
            settings_frame,
            text="⏹ 停止",
            command=self.stop_processing,
            fg_color="#ff4444",
            state="disabled"
        )
        self.stop_btn.pack(fill="x", padx=10, pady=5)

    def setup_progress_panel(self):
        progress_frame = ctk.CTkFrame(self.right_frame)
        progress_frame.pack(fill="x", pady=10)

        ctk.CTkLabel(progress_frame, text="整體進度:").pack(anchor="w", padx=10)
        self.overall_progress = ctk.CTkProgressBar(progress_frame)
        self.overall_progress.pack(fill="x", padx=10, pady=5)
        self.overall_progress.set(0)

        self.progress_label = ctk.CTkLabel(progress_frame, text="0%")
        self.progress_label.pack(anchor="e", padx=10)

    def setup_log_panel(self):
        log_frame = ctk.CTkFrame(self.right_frame)
        log_frame.pack(fill="both", expand=True, pady=10)

        ctk.CTkLabel(log_frame, text="處理日誌:").pack(anchor="w", padx=10)
        self.log_textbox = ctk.CTkTextbox(log_frame, height=200)
        self.log_textbox.pack(fill="both", expand=True, padx=10, pady=5)

    def browse_folder(self):
        from tkinter import filedialog
        folder = filedialog.askdirectory()
        if folder:
            self.folder_entry.delete(0, "end")
            self.folder_entry.insert(0, folder)
            self.scan_videos()

    def scan_videos(self):
        # 掃描資料夾中的影片
        pass

    def start_processing(self):
        # 在背景執行緒中執行翻譯
        self.processing = True
        self.start_btn.configure(state="disabled")
        self.stop_btn.configure(state="normal")

        thread = Thread(target=self.run_translation, daemon=True)
        thread.start()

        # 啟動 UI 更新
        self.update_ui()

    def run_translation(self):
        # 呼叫 translate_video.py 中的翻譯邏輯
        pass

    def stop_processing(self):
        self.processing = False
        self.start_btn.configure(state="normal")
        self.stop_btn.configure(state="disabled")

    def update_ui(self):
        # 定期更新 UI (進度、日誌)
        if self.processing:
            # 檢查日誌佇列
            while not self.log_queue.empty():
                msg = self.log_queue.get()
                self.log_textbox.insert("end", msg + "\n")
                self.log_textbox.see("end")

            self.parent.after(100, self.update_ui)
```

---

## 五、與現有程式碼整合

### 5.1 整合 translate_video.py

```python
# gui/utils/translation_runner.py
import sys
import os
sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from translate_video import TranscriptionPipeline, main as translate_main
from queue import Queue

class TranslationRunner:
    """GUI 專用的翻譯執行器，提供進度回調"""

    def __init__(self, progress_callback=None, log_callback=None):
        self.progress_callback = progress_callback
        self.log_callback = log_callback
        self.cancelled = False

    def run(self, video_files: list, config: dict):
        """執行翻譯流程"""
        total = len(video_files)

        for i, video_file in enumerate(video_files):
            if self.cancelled:
                break

            # 更新進度
            if self.progress_callback:
                self.progress_callback((i + 1) / total, video_file)

            # 執行翻譯
            # ... 調用現有的翻譯邏輯

            # 輸出日誌
            if self.log_callback:
                self.log_callback(f"完成處理: {video_file}")

    def cancel(self):
        self.cancelled = True
```

### 5.2 整合配置系統

```python
# gui/utils/config_manager.py
import json
import os

class ConfigManager:
    """管理 translation_config.json"""

    CONFIG_PATH = "translation_config.json"

    @classmethod
    def load(cls) -> dict:
        if os.path.exists(cls.CONFIG_PATH):
            with open(cls.CONFIG_PATH, 'r', encoding='utf-8') as f:
                return json.load(f)
        return cls.get_defaults()

    @classmethod
    def save(cls, config: dict):
        with open(cls.CONFIG_PATH, 'w', encoding='utf-8') as f:
            json.dump(config, f, ensure_ascii=False, indent=2)

    @classmethod
    def get_defaults(cls) -> dict:
        return {
            "whisper": {
                "model": "base",
                "device": "auto",
                "engine": "faster-whisper"
            },
            "translation": {
                "provider": "deepseek"
            },
            # ...
        }
```

---

## 六、執行緒處理

為了保持 GUI 響應性，所有耗時操作必須在背景執行緒中執行：

```python
# gui/utils/threading_utils.py
from threading import Thread
from queue import Queue
import customtkinter as ctk

class BackgroundTask:
    """背景任務執行器"""

    def __init__(self, app: ctk.CTk):
        self.app = app
        self.result_queue = Queue()

    def run(self, func, callback=None, *args, **kwargs):
        """在背景執行函式，完成後在主執行緒呼叫 callback"""

        def worker():
            try:
                result = func(*args, **kwargs)
                self.result_queue.put(("success", result))
            except Exception as e:
                self.result_queue.put(("error", str(e)))

        def check_result():
            if not self.result_queue.empty():
                status, result = self.result_queue.get()
                if callback:
                    callback(status, result)
            else:
                self.app.after(100, check_result)

        thread = Thread(target=worker, daemon=True)
        thread.start()
        self.app.after(100, check_result)
```

---

## 七、實作優先順序

### Phase 1: 基礎框架 (必要)
1. ✅ 主視窗框架和 TabView
2. ✅ 設定 Tab (配置管理)
3. ✅ 基本主題樣式

### Phase 2: 核心功能 (高優先)
4. 翻譯處理 Tab
   - 影片列表顯示
   - 進度顯示
   - 開始/停止控制
   - 日誌輸出

### Phase 3: 編輯功能 (中優先)
5. 字幕編輯 Tab
   - 字幕載入/儲存
   - 搜尋替換

6. 位置調整 Tab
   - 滑桿控制
   - 視覺預覽

### Phase 4: 進階功能 (低優先)
7. IG 文案 Tab
   - 文案編輯
   - AI 生成整合

8. 打包成 .exe
   - PyInstaller 配置
   - 圖示和元資料

---

## 八、依賴需求

```
# requirements-gui.txt
customtkinter>=5.2.0
Pillow>=9.0.0        # 圖片處理 (圖示、預覽)
pyinstaller>=5.0     # 打包為 .exe (可選)
```

安裝指令:
```bash
pip install customtkinter Pillow
```

---

## 九、啟動方式

```python
# gui/main.py
import customtkinter as ctk
from app import VideoTranslateApp

def main():
    app = VideoTranslateApp()
    app.mainloop()

if __name__ == "__main__":
    main()
```

命令列啟動:
```bash
python -m gui.main
```

或建立批次檔:
```batch
@echo off
python -m gui.main
pause
```

---

## 十、注意事項

1. **執行緒安全**: 所有 GUI 更新必須在主執行緒中執行，使用 `after()` 方法
2. **配置同步**: GUI 設定變更需即時寫入 `translation_config.json`
3. **錯誤處理**: 所有背景任務需要適當的例外處理和使用者提示
4. **資源釋放**: 視窗關閉時需正確終止所有背景執行緒
5. **跨平台**: CustomTkinter 支援 Windows/macOS/Linux，但主要針對 Windows 優化

---

*文檔版本: 1.0*
*建立日期: 2026-01-08*
